let age = prompt("How old are you?");
let result = (age * 365);
let resultB = (age * 366);
console.log(age * 365 );
console.log(age * 365.25);

alert(` ${age} * years is roughly ${result} days  and ${resultB} days for every leap year`)
let no = (6 != 5);
